import {
  // create naive ui
  create,
  // component
  NButton,
  NCard,
  NLayout,
  NLayoutContent,
  NLayoutHeader,
  NLayoutSider,
} from "naive-ui";
export default create({
  components: [
    NButton,
    NLayout,
    NLayoutContent,
    NLayoutHeader,
    NLayoutHeader,
    NLayoutSider,
    NCard,
  ],
});
